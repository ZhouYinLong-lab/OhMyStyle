# {{中文工作流名称}}

> 这是一个创作工作流，不是风格包。它规定如何观察、转译和编排输入素材；需要视觉语言时，再单独选择 OhMyStyle 风格包。

## 适用场景

{{用几句话说明输入素材、转化目标和不应被固定的内容。}}

## 工作流边界

- 默认主体必须保持开放，`subject_policy.default_subjects` 必须为空。
- 输入素材的主体、身份和用户约束优先于版式或风格处理。
- `style_projection.allowed_axes` 只列出允许改变的视觉维度。
- 具体地点、人物、物件和故事只能来自用户输入或明确选择，不得写进默认 Prompt。
- 需要引用外部项目时，只登记链接和许可证，不复制其 Skill、Prompt、示例图或品牌素材。

## 使用方式

1. 把本目录交给具备生图能力的 Agent，并说明你的输入素材与输出目标。
2. 直接复制 `workflow.yaml` 中的约束，替换主题和画幅；不要删除主体保护条件。
3. 将工作流交给支持 API 的生图 Agent，配置你自己的 API Key，由 Agent 编译并生成。
4. 使用本地模型和 ComfyUI 时，把工作流规则、参考图清单和输出尺寸分别接入工作流节点。

生成前确认输入素材的授权范围；生成后检查主体是否被替换、是否出现未要求的固定物件，以及输出是否满足画幅要求。
