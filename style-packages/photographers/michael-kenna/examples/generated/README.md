# Generated examples

This is the staging area for new anonymous examples. Move an image to
`accepted/` only after it passes the package evaluation and receives a human
review record.

For subject-independence checks, keep one output record per neutral benchmark
task in this directory. If no image provider was available, record `pending`
and the provider-neutral prompt instead of inventing a score or an image.
