# Secondary references

The second screenshot from each game is used as a cross-scene check. It tests
whether the package describes transferable pixel-game rules instead of one
remembered composition or palette.
