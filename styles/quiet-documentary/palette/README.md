# Palette assets

`palette.json` is the machine-readable palette. A `swatches.png` may be added
later only if it is created for this package or has a license that allows
redistribution. The package currently contains no raster swatch image.
